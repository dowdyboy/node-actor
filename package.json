{
  "name": "node-actor",
  "version": "1.0.1",
  "description": "an actor model implement for nodejs",
  "scripts": {
    "test": "test",
    "start-node": "gulp clean && tsc && gulp tsc && cd dist && node index.js",
    "start-ts-node": "cd src && ts-node index.ts",
    "watch-node": "nodemon --watch src/**/* -e * --exec \"gulp clean && tsc && gulp tsc && cd dist && node index.js\"",
    "watch-ts-node": "nodemon --watch src/**/* -e * --exec \"cd src && ts-node index.ts\"",
    "build": "gulp clean && tsc && gulp tsc && gulp build",
    "clean": "gulp clean"
  },
  "author": "dowdyboy",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^10.12.5",
    "del": "^3.0.0",
    "gulp": "^3.9.1",
    "gulp-uglify": "^3.0.1",
    "nodemon": "^1.18.6",
    "ts-node": "^7.0.1",
    "typescript": "^3.1.6"
  }
}
